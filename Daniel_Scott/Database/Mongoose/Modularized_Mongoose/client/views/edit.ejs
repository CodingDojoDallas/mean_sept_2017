<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Edit Your Animal</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="css/style.css" rel="stylesheet">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta/css/bootstrap.min.css" integrity="sha384-/Y6pD6FV/Vv2HJnA6t+vslU6fwYXjCFtcEpHbNJ0lyAFsXTsjBbfaDjzALeQsN6M" crossorigin="anonymous">
    </head>
    <body>
        <div class="container">
            <h2>Edit a Animal Here!</h2>
            <h3>Name: <%= animals.name %></h3>
            <h4>Description: <%= animals.description %></h4>
            <h5>Created At: <%= animals.createdAt %></h5>
             <% if(typeof(errors) != 'undefined' ) { %>
                <% for (var x in errors) { %>
                <h3><%= errors[x].message %></h3>
                <% } %>
            <% } %>

            <form action="/otters/<%= animals._id %>" method='post'>
                <label for='name'>Edit Name: </label>
                <input name='name' id='name' type='text' value="<%= animals.name %>">

                <label for='description'>Edit Description: </label>
                <textarea name='description'><%= animals.description %></textarea>

                <input type='submit' value='Edit this Animal'>


            </form>
        </div>
    </body>
</html>
